services:
  app:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    ports:
      - "5001:5001"
    environment:
      - NODE_ENV=development
    volumes:
      - ..:/app
      - /app/node_modules